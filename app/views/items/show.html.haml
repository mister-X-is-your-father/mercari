- if user_signed_in?
  - if @item.user_id == current_user.id
    = render partial: 'layouts/item-show-user'
  - else
    = render partial: 'layouts/item-show-not-user'
- else
  = render partial: 'layouts/item-show-not-user'